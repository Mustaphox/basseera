# النشر على InfinityFree

1. من لوحة InfinityFree، افتح **MySQL Databases** ثم **phpMyAdmin** لقاعدة البيانات المخصصة للموقع.
2. اختر قاعدة البيانات ثم من تبويب **Import** ارفع الملف `database/schema.sql` ونفّذ الاستيراد. لا تستخدم `setup_db.php`؛ فهو مخصص للتطوير ومغلق في النسخة المنشورة.
   - إذا كان اسم الموقع أو وصفه يظهر كعلامات استفهام بعد استيراد سابق، استورد بعدها `database/fix_site_settings.sql` مرة واحدة.
3. عبر مدير الملفات أو FTP، ارفع محتويات هذا المشروع نفسها داخل مجلد `htdocs` (وليس مجلد المشروع كطبقة إضافية).
4. تأكد من رفع الملف `config/database.local.php` مع الملفات. يحتوي هذا الملف على إعدادات قاعدة البيانات ولا يجب رفعه إلى Git أو مشاركته.
5. افتح نطاق الموقع. الروابط تعمل تلقائياً سواء كان الموقع في `htdocs` أو داخل مجلد فرعي.
6. بعد أول دخول إلى لوحة الإدارة، غيّر كلمة مرور الحساب الافتراضي فوراً.
7. في Google Search Console، أرسل `https://basira.is-best.net/sitemap.xml` من قسم **Sitemaps**.

## فحص سريع عند ظهور خطأ 500

- تحقق أن استيراد `database/schema.sql` اكتمل بلا أخطاء.
- تحقق من وجود `config/database.local.php` على الاستضافة وأن بياناته تطابق بيانات MySQL في لوحة InfinityFree.
- ارفع ملف `.htaccess` أيضاً؛ فهو ضروري للروابط النظيفة والحماية ومنع فهرسة المجلدات.
